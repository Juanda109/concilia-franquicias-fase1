apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Fijado aquí también (no solo en el kustomization.yaml raíz) para que
# aplicar esta carpeta sola (kubectl kustomize IaC/bucket) no dependa de
# cuál sea tu "oc project" activo en ese momento.
namespace: ${OKD_NAMESPACE}

resources:
  - 00-secret-template.yaml
  - 01-pvc.yaml
  - 02-deployment.yaml
  - 03-service.yaml
