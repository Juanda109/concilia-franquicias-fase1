apiVersion: v1
kind: Service
metadata:
  name: elasticsearch-service
  labels:
    app.kubernetes.io/part-of: elk-metrics
spec:
  selector:
    app: elasticsearch
  ports:
  - protocol: TCP
    port: 9200
    targetPort: 9200