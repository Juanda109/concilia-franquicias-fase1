apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - 00-configmap.yaml
  - 01-cronjob.yaml
  # Dataset de doble cobro (28 casos) para el benchmark completo.
  - 03-configmap-dataset.yaml
  # Canario de ruteo (suspendido; activarlo es decision de negocio).
  - 02-cronjob-canario.yaml
