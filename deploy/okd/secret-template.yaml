apiVersion: v1
kind: Secret
metadata:
  name: concilia-secrets
type: Opaque
stringData:
  DATABASE_URL: "${DATABASE_URL}"
  SFTP_USERNAME: "${SFTP_USERNAME}"
  SFTP_PASSWORD: "${SFTP_PASSWORD}"
# No guardar credenciales reales en Git.
